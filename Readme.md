# PR Reviewer Assignment Service  
Этот сервис автоматически назначает ревьюверов на Pull Request'ы, позволяет управлять командами и активностью пользователей. Взаимодействие происходит через HTTP API

---

## Функциональность

- Создание команд с участниками  
- Управление активностью пользователей  
- Создание Pull Request'ов и автоматическое назначение до 2 ревьюверов  
- Переназначение ревьюверов с учетом доменных правил  
- Идемпотентный merge PR  
- Получение PR'ов, назначенных конкретному пользователю  

---

## Технологии

- **Language:** Go
- **Database:** PostgreSQL (через docker-compose)
- **Containerization:** Docker + Docker Compose

---

## 📦 Запуск проекта

Требования:
- Docker
- Docker Compose

Запуск сервисов:

```bash
make up
````

или

```bash
docker-compose up
```

Остановка сервисов:

```bash
make down
```

или

```bash
docker-compose down
```

После запуска сервис будет доступен по адресу:

```
http://localhost:8080/
```

---

## Основные эндпоинты

| Метод | Путь                    | Описание                           |
| ----- | ----------------------- | ---------------------------------- |
| POST  | `/team/add`             | Создать команду                    |
| GET   | `/team/get`             | Получить команду                   |
| POST  | `/users/setIsActive`    | Установить активность пользователя |
| POST  | `/pullRequest/create`   | Создать PR и назначить ревьюверов  |
| POST  | `/pullRequest/merge`    | Пометить PR как MERGED             |
| POST  | `/pullRequest/reassign` | Переназначить ревьювера            |
| GET   | `/users/getReview`      | Получить список PR для ревьювера   |

---

## Принятые решения и допущения

 **1. Использование строковых ID вместо UUID**  
В OpenAPI спецификации идентификаторы сущностей (`user_id`, `team_name`, `pull_request_id`) указаны как обычные строки.  
Я считаю, что в реальных системах предпочтительно использовать UUID, однако в рамках тестового задания использовал строковые ID строго по спецификации, чтобы сохранить соответствие API-контракту.

---
 **2. Создание PR неактивным пользователем**  
В спецификации отсутствует требование, что пользователь с `is_active = false` не может создавать Pull Request, а также нет определения соответствующей ошибки.  
Поэтому реализовано следующее: **неактивный пользователь может создать PR**, однако он **никогда не может быть назначен ревьювером**.

---

**3. Поведение при создании команды и обновлении участников**  
Согласно OpenAPI, при попытке создать уже существующую команду необходимо вернуть ошибку `TEAM_EXISTS`.  
При создании новой команды пользователи либо создаются, либо их данные обновляются, включая смену принадлежности к команде.  
Если в результате обновления участников старая команда остается без членов, она **не удаляется**, так как такое поведение не описано в требованиях.  
Такая команда остаётся в системе в состоянии **без участников**, поскольку функционала удаления команд в задании не предусмотрено.

---
 **4. Ответ метода `/users/getReview`**  
Спецификация указывает только статус `200 OK`, даже если у пользователя нет PR или пользователь отсутствует.  
Реализация следует спецификации метод **всегда** возвращает `200 OK` и список PR (возможен пустой).  
Однако, возможно более логичным было бы возвращать `404` при запросе несуществующего пользователя. Такой вариант отмечен в коде в комментариях как потенциал для улучшения.
